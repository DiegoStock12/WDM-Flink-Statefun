syntax = "proto3";

package orders;

// State of the user
message UserOrder{
    string name = 1;
    int64 count = 2;
    int64 id = 3;
}

// State of the order
message Order{
    int64 id = 1;
    int64 userId = 2;
    repeated int64 items = 3;
}

message CreateOrder{
    int64 userId = 1;
}
